# =================================================================
# Sathyabama Email Service Configuration
# =================================================================

# Server Configuration
server.port=8081
server.servlet.context-path=/email-service

# Application Configuration
spring.application.name=Sathyabama Email Service

# Email Configuration - Gmail SMTP
# IMPORTANT: Replace with your actual email credentials
# For Gmail: Use App Password, not regular password
# Generate App Password: https://myaccount.google.com/apppasswords
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mpraveen.k2007@gmail.com
spring.mail.password=lvde ftyz kmew tolu
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Logging Configuration
logging.level.com.sathyabama.email=DEBUG
logging.level.org.springframework.mail=DEBUG

# Profile Settings
# Use 'dev' for development (logs emails only)
# Use 'prod' for production (sends real emails)
spring.profiles.active=prod

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost
spring.web.cors.allowed-methods=GET,POST,OPTIONS
spring.web.cors.allowed-headers=*

# Email From Configuration
app.email.from=mpraveen.k2007@gmail.com
app.email.from-name=Sathyabama Event Management System